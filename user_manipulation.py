from werkzeug.security import generate_password_hash
import sqlite3

# Add user

# username = "dummy"
# password = "dummypassword"
#
# password = generate_password_hash(password)
# con = sqlite3.connect("instance/chat_app.db")
# cur = con.cursor()
# cur.execute(f"""INSERT INTO USER(USERNAME, PASSWORD) VALUES ('{username}', '{password}')""")
# con.commit()

# Delete user

# username = "dummy"
# con = sqlite3.connect("instance/chat_app.db")
# cur = con.cursor()
# cur.execute(f"""DELETE FROM USER WHERE USERNAME == '{username}'""")
# con.commit()


